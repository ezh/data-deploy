apiVersion: v1
kind: Service
metadata:
  name: data-notifier
  labels:
    name: data-notifier-service
spec:
  clusterIP: 10.0.0.113
  ports:
    - port: 8999
      name: api
  selector:
    app: data
    component: data-notifier
